[package]
name = "quakespan"
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
publish = false

[lib]
name = "quakespan"
path = "src/lib.rs"

[profile.dev]
overflow-checks = true

[profile.release]
debug = 1
overflow-checks = true
panic = "abort"
